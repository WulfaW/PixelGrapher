{
  "builder": "nixpacks",
  "nixpacks": {
    "buildCommand": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build && mkdir -p dist/server/public && cp -r ../frontend/dist/* dist/server/public/"
  }
}
